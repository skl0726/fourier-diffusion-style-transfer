# 작업하면서 발생하는 패키지 파일 정리 (특정 코드에서 에러 발생할 경우 기록해주기! 기록 없으면 충돌 없는 거로 생각하기)

transforms==4.46.3
torchvision==0.19.1

# 새로운 폴더 만들고 거기에 코드 넣은 다음 임포트할 때 인식 안 되면 다음과 같이 진행: "pip uninstall -y latent-diffusion && pip install -e ."